✒️ 凡人智能写作系统 (MortalWrite)





一款专为网络小说家、编剧和创作者设计的桌面智能写作助手，它将强大的AI能力与完全本地化的数据管理相结合，让您的灵感安全、高效地转化为文字。

请在此处替换为您的软件截图


✨ 项目简介
在 AI 浪潮下，写作工具层出不穷，但大多依赖云端，让创作者对自己的数据隐私和长期存续感到不安。凡人 (MortalWrite) 的诞生就是为了解决这个问题。它是一个运行在您自己电脑上的桌面软件，拥有媲美在线工具的 AI 功能，同时确保您的每一本书、每一个角色、每一个灵感都只属于您自己。

本软件基于 Python 和 Streamlit 构建，并使用 PyWebView 和 PyInstaller 打包成原生桌面应用，实现了跨平台的免安装、免浏览器体验。

🚀 主要功能
📚 书籍与章节管理:

以“书籍”为核心，轻松管理多部小说。

从 TXT 文件智能导入书籍，自动识别并拆分 篇-卷-章 结构。

通过拖拽方便地调整章节、分卷的顺序。

支持对整本书或特定章节进行 TXT 格式的导出。

✍️ 沉浸式 AI 写作:

简洁的写作界面，支持根据大纲或提示词进行 流式 AI 续写。

强大的 AI 批量生成 功能，可根据通用指令一次性生成多个章节。

写作辅助：内置“矛盾检测”和“一键重写”功能，帮助优化章节逻辑和文笔。

👥 角色档案与关系图谱:

为每个角色建立详细档案，包括姓名、定位、性别、种族、描述和头像。

AI 智能提取：一键分析书籍内容，自动发现并创建新角色。

AI 生成人物关系图谱：调用 AI 分析所有角色，自动生成并可视化人物关系网络图，让复杂关系一目了然。

💡 灵感风暴:

当您卡文时，选择“生成目标”（如核心脑洞、金手指设计、剧情反转等），并输入关键词，AI 会为您提供多个创意方向。

所有生成的灵感都可以被编辑、备注并存档，形成您的私人创意库。

🧠 拆书知识库 (风格分析):

上传您喜欢的文章或小说片段，AI 会自动分析其“文笔DNA”（如句式、用词、节奏、氛围等）。

利用已提炼的风格，对您自己的章节进行 风格化改造，学习和模仿大师文笔。

📊 创作数据中心:

提供全局和单书维度的统计看板，包括总字数、总章节数、角色数等核心指标。

通过交互式图表展示 每日码字趋势 和 AI 花费趋势。

详细列出每本书的字数、章节数和单章平均字数。

🤖 高度灵活的模型调度:

多厂商支持：内置支持 OpenAI, Anthropic, DeepSeek, 阿里云, Google, 百度, 月之暗面, 豆包, SiliconFlow 等多家主流 AI 服务商。

自定义模型：支持添加任何与 OpenAI API 兼容的自定义模型。

功能级调度：可以为“快速续写”、“批量生成”、“风格分析”等 每一个具体功能 指派不同的 AI 模型，实现成本与效果的最佳平衡。

🔒 安全与个性化:

100% 数据本地化：您的所有创作数据、AI 配置和日志都存储在本地 SQLite 数据库中，无数据上传，保障隐私。

个性化主题：内置多种 UI 主题（如翡翠森林、紫气东来、永夜极光），支持一键切换。

桌面端应用: 被打包为 .exe 可执行文件，提供原生桌面应用体验，无需依赖浏览器。

🛠️ 技术栈
核心框架: Python

用户界面: Streamlit

桌面封装: pywebview (将 Web 界面打包成原生窗口)

AI 交互: OpenAI SDK

数据存储: SQLite (本地数据库)

数据处理: Pandas, NumPy

图表可视化: Plotly (数据看板), Pyvis (人物关系图)



🎯 快速开始
面向普通用户
您无需安装 Python 或任何复杂的环境。

前往本项目的 Releases 页面。

下载最新版本的压缩包（例如 MortalWrite_v1.0.zip）。

解压后，双击运行文件夹内的 MortalWrite.exe 即可开始使用。

首次启动时，程序会提示您选择一个“工作区”文件夹，您的所有数据都将保存在那里。

提示：您可以为 MortalWrite.exe 创建一个桌面快捷方式，方便日后启动。

面向开发者
如果您想从源码运行或进行二次开发，请按以下步骤操作：

克隆仓库

bash
git clone https://github.com/YOUR_USERNAME/MortalWrite.git
cd MortalWrite
创建并激活虚拟环境 (推荐)

bash
python -m venv venv
# Windows
.\venv\Scripts\activate
# macOS/Linux
source venv/bin/activate
安装依赖

bash
pip install -r requirements.txt
从源码运行

bash
streamlit run main.py
应用将在您的默认浏览器中打开。

📖 使用指南
设置工作区：首次启动时，请选择一个安全、稳定的文件夹作为您的工作区。所有的小说、角色、配置和日志都会保存在这里。首次启动时需要等待几十秒启动时间。

配置 AI 模型：

进入 【系统设置】 -> 【模型配置与调度中心】。

选择您要使用的 AI 厂商（如 OpenAI）。

填入您的 API Key 和 Base URL（如果需要代理）。

在 【功能调度】 部分，为不同的功能（如“沉浸写作”、“灵感生成”）分配合适的模型。

点击 【保存所有配置】。

开始创作：

在 【书籍管理】 中创建或导入您的第一本书。

进入 【沉浸写作】，在左侧目录选择章节，然后在右侧开始您的创作之旅！

🤝 贡献代码
欢迎所有形式的贡献！如果您有好的想法、发现了 BUG 或想优化代码，请：

Fork 本仓库。

创建一个新的功能分支 。

提交您的修改 。

推送到您的分支。

创建一个 Pull Request。

您也可以直接提交 Issues 来报告问题或提出建议。

📄 许可证
本项目采用 MIT 许可证。详情请见 LICENSE 文件。

🙏 致谢
感谢 Streamlit, PyWebView, PyInstaller, Pyvis 等优秀开源项目的开发者。

感谢所有为本项目提供灵感和反馈的用户。